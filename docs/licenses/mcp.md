# Optional MCP Python SDK Dependency

AECCTX's optional stdio wrapper uses `mcp>=1.20,<2`, whose installed package metadata classifies it under the MIT License.

It is installed only through `aecctx[mcp]` and is not required by the core library or CLI.
