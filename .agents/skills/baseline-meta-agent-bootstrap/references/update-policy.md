# Update Policy

Bootstrap is full integration, not scaffolding.

If the repo already has baseline integration, prefer the sync skill instead of re-bootstrap.

If the repo posture is already known, choose the specialization profile during bootstrap instead of leaving the repo on neutral defaults by omission.
