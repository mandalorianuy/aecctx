# Baseline Offer

Bootstrap gives the consumer repo:
- baseline shell contract composition
- explicit `baseline-shared-v1` bundle pin
- baseline-owned specialization profiles so the consumer can choose posture without forking baseline defaults
- managed overlay under `.agent_baseline/manifests/`
- managed governance state and integration report
- local copies of the 3 baseline meta-agent skills
