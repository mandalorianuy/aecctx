# ACX-31 mesh CRS fixtures

These project-authored, Apache-2.0 fixtures exercise the bounded offline CRS profile. `nad83-triangle.obj` stores latitude, longitude, and ellipsoidal height in the native axis order required by the selected EPSG:1252 operation. `crs-registry.json` is reproducibly generated by `generate_fixtures.py` from the exact governed pyproj/PROJ/EPSG runtime.

The fixture is synthetic and establishes neither survey authority nor real-world location.
