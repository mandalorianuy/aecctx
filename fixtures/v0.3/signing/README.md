# ACX-35 advanced signing fixtures

All certificates, CRLs, private keys, signatures and time assertions in this directory are deterministic project-generated test material. They are Apache-2.0 test fixtures only, are not production identities or trust roots, and MUST NOT be used to protect real data.

Regenerate with `python fixtures/v0.3/signing/generate_fixtures.py`; verify with `--check`.
