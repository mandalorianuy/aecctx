---
aecctx_version: 0.1.0-draft
package_id: pkg_minimal_fixture
generator: aecctx-fixture/0.1.0-draft
profile: agent
---

# Minimal AECCTX fixture

One DXF `LINE` primitive was extracted from `model-space/handle:1A`. Its source layer is `A-WALL`, but the fixture does not claim that the line is a wall.

Capabilities: identity and text are full; 2D geometry and properties are partial; 3D geometry is unsupported.

Authoritative records are under `sources/`, `evidence/`, and `model/`.
