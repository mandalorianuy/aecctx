# AECCTX v0.2 shared evidence fixture

This generated projection points to `src_v02_shared`, `prim_inference_1`, and `assert_manual_units_1` in the authoritative JSONL records.

It is not authority for inference, coordinates, fidelity, provider attestation, or diagnostics.
