# ACX-11 shared v0.2 fixtures

`minimal-v02/` is a hand-authored, Apache-2.0-project fixture containing no third-party or proprietary source material. It demonstrates the shared v0.2 record envelope only; it does not claim a format adapter capability.

The fixture includes:

- observed source identity;
- an inferred primitive with separate extraction/interpretation confidence and deterministic provider provenance;
- an explicit manual-units assertion separate from declared source units;
- unknown global CRS state;
- inferred/derived representation fidelity;
- a generated Markdown projection that points back to authoritative JSONL records.

Manifest SHA-256: `c92fec63d28520f6857f331968aab3fdf3866d579fcdeeab4cf483c6c1b5226e`.
